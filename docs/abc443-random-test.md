# abc443 ランダムテスト結果

実行日: 2026-04-19 | --random 5 --no-test

## 問題 a

**結果**: AC:5/5  max: 2 ms  
**スキップ制約**: (なし)  

**考察**:
全ケースACした（クラッシュ・TLEなし）。解法未実装のため空出力だが、入力生成は正常に機能している。

**stdin例（3件）**:

ケース1 (`corner1`) — Accepted (2ms):
```
zkesxrowua
```

ケース2 (`corner2`) — Accepted (0ms):
```
a
```

ケース3 (`corner3`) — Accepted (0ms):
```
rxonbhejc
```

---

## 問題 b

**結果**: AC:5/5  max: 2 ms  
**スキップ制約**: (なし)  

**考察**:
全ケースACした（クラッシュ・TLEなし）。解法未実装のため空出力だが、入力生成は正常に機能している。

**stdin例（3件）**:

ケース1 (`corner1`) — Accepted (2ms):
```
100000000 100000000
```

ケース2 (`corner2`) — Accepted (0ms):
```
1 1
```

ケース3 (`corner3`) — Accepted (0ms):
```
25816084 1387387
```

---

## 問題 c

**結果**: AC:5/5  max: 140 ms  
**スキップ制約**: (なし)  

**考察**:
全ケースACした（クラッシュ・TLEなし）。解法未実装のため空出力だが、入力生成は正常に機能している。

**stdin例（3件）**:

ケース1 (`corner1`) — Accepted (140ms):
```
200000 1000000000
...(省略)
```

ケース2 (`corner2`) — Accepted (0ms):
```
0 1000000000
```

ケース3 (`corner3`) — Accepted (0ms):
```
1 1000000000
1000000000
```

---

## 問題 d

**結果**: AC:1 RE:4 TLE:0  max: 96 ms  
**スキップ制約**: (なし)  

**考察**:
**sum制約対応調査**。制約は `1 <= T <= 50000`、`2 <= N <= 3e5`、「ひとつの入力について、Nの総和は3×10^5を超えない」。パーサーは「の総和は」+「を超えない」の形式も対応しているが、corner1でT=50000、N=200000が生成されており、sum制約（T=50000時N_hi=6）が適用されていない。HTMLテキストの前置きパターン（「ひとつの入力について、」）により制約行の正規化後の形式が想定と異なる可能性がある。要調査。

**stdin例（3件）**:

ケース1 (`corner1`) — Runtime Error (exit status: 101) (96ms):
```
50000
200000
...(省略)
```

ケース2 (`corner2`) — Accepted (43ms):
```
1
200000
...(省略)
```

ケース3 (`corner3`) — Runtime Error (exit status: 101) (89ms):
```
3398
300000
...(省略)
```

---

## 問題 e

**結果**: AC:0 RE:5 TLE:0  max: 2 ms  
**スキップ制約**: (なし)  

**考察**:
制約は `1 <= T,N <= 5e4`。corner1はT=50000、N=50000。全5件RE。解法未実装。

**stdin例（3件）**:

ケース1 (`corner1`) — Runtime Error (exit status: 101) (2ms):
```
50000
3000 3000
...(省略)
```

ケース2 (`corner2`) — Runtime Error (exit status: 101) (0ms):
```
1
3000 1
#
#
.
```

ケース3 (`corner3`) — Runtime Error (exit status: 101) (0ms):
```
4120
3000 2650
...(省略)
```

---

## 問題 f

**結果**: AC:5/5  max: 2 ms  
**スキップ制約**: (なし)  

**考察**:
全ケースACした（クラッシュ・TLEなし）。解法未実装のため空出力だが、入力生成は正常に機能している。

**stdin例（3件）**:

ケース1 (`corner1`) — Accepted (2ms):
```
3000000
```

ケース2 (`corner2`) — Accepted (0ms):
```
1
```

ケース3 (`corner3`) — Accepted (0ms):
```
2944913
```

---

## 問題 g

**結果**: AC:1 RE:4 TLE:0  max: 2 ms  
**スキップ制約**: (なし)  

**考察**:
制約は `1 <= N <= 3e5`。corner1はN=300000の最大サイズ。AC1件（corner2=小規模）、RE4件。解法未実装。

**stdin例（3件）**:

ケース1 (`corner1`) — Runtime Error (exit status: 101) (2ms):
```
300000
1000000000 1000000000 1000000000 1000000000
```

ケース2 (`corner2`) — Accepted (0ms):
```
1
1 1 0 0
```

ケース3 (`corner3`) — Runtime Error (exit status: 101) (0ms):
```
207123
907655424 982839803 213734670 795792911
```

---
