# abc446 ランダムテスト結果

実行日: 2026-04-19 | --random 5 --no-test

## 問題 a

**結果**: AC:5/5  max: 2 ms  
**スキップ制約**: (なし)  

**考察**:
全ケースACした（クラッシュ・TLEなし）。解法未実装のため空出力だが、入力生成は正常に機能している。

**stdin例（3件）**:

ケース1 (`corner1`) — Accepted (2ms):
```
1000000000
```

ケース2 (`corner2`) — Accepted (0ms):
```
1
```

ケース3 (`corner3`) — Accepted (0ms):
```
306530716
```

---

## 問題 b

**結果**: BUILD失敗  
**スキップ制約**: (なし)  

**考察**:
ビルドに失敗。コンパイルエラーがあり入力生成は実行されていない。

**stdin例（0件）**:

---

## 問題 c

**結果**: AC:1 RE:4 TLE:0  max: 2 ms  
**スキップ制約**: (なし)  

**考察**:
制約は `1 <= N <= 2e5`。corner1はN=200000の最大。AC1件（corner2=最小）、RE4件。解法未実装。

**stdin例（3件）**:

ケース1 (`corner1`) — Runtime Error (exit status: 101) (2ms):
```
200000
1 1
10
10
```

ケース2 (`corner2`) — Accepted (0ms):
```
1
1 1
1
1
```

ケース3 (`corner3`) — Runtime Error (exit status: 101) (0ms):
```
26775
1 1
3
4
```

---

## 問題 d

**結果**: AC:5/5  max: 144 ms  
**スキップ制約**: (なし)  

**考察**:
全ケースACした（クラッシュ・TLEなし）。解法未実装のため空出力だが、入力生成は正常に機能している。

**stdin例（3件）**:

ケース1 (`corner1`) — Accepted (144ms):
```
200000
...(省略)
```

ケース2 (`corner2`) — Accepted (0ms):
```
1
1
```

ケース3 (`corner3`) — Accepted (0ms):
```
1
488450297
```

---

## 問題 e

**結果**: AC:5/5  max: 2 ms  
**スキップ制約**: (なし)  

**考察**:
全ケースACした（クラッシュ・TLEなし）。解法未実装のため空出力だが、入力生成は正常に機能している。

**stdin例（3件）**:

ケース1 (`corner1`) — Accepted (2ms):
```
1000 999 999
```

ケース2 (`corner2`) — Accepted (0ms):
```
2 0 0
```

ケース3 (`corner3`) — Accepted (0ms):
```
196 172 183
```

---

## 問題 f

**結果**: AC:5/5  max: 247 ms  
**スキップ制約**: (なし)  

**考察**:
全ケースACした（クラッシュ・TLEなし）。解法未実装のため空出力だが、入力生成は正常に機能している。

**stdin例（3件）**:

ケース1 (`corner1`) — Accepted (247ms):
```
300000 200000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
300000 300000
...(省略)
```

ケース2 (`corner2`) — Accepted (0ms):
```
2 1
1 1
```

ケース3 (`corner3`) — Accepted (0ms):
```
288625 1
9920 251534
```

---

## 問題 g

**結果**: AC:2 RE:3 TLE:0  max: 125 ms  
**スキップ制約**: (なし)  

**考察**:
解法が未実装のためREが発生しているが、入力の形式・制約は問題の要件に対して妥当と考えられる。

**stdin例（3件）**:

ケース1 (`corner1`) — Accepted (100ms):
```
200000
...(省略)
```

ケース2 (`corner2`) — Accepted (42ms):
```
200000
...(省略)
```

ケース3 (`corner3`) — Runtime Error (exit status: 101) (124ms):
```
1000000000
...(省略)
```

---
